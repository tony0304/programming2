// 006
// 輸入每月網內、網外、市話、通話時間(sec)及網內、網外 簡訊則數，求最佳資費。
// 費率如下表：
// *月租費也要納入考量
// 資費類型 183型 383型 983型
// 月租費 183元 383元 983元
// 優惠內容 月租費可抵等額通信費
// 語音 網內 0.08 0.07 0.06
// (元/秒) 網外 0.1393 0.1304 0.1087
// 市話(元/秒) 0.1349 0.1217 0.1018
// 簡訊 網內 1.1287 1.1127 0.9572
// (元/則) 網外 1.4803 1.2458 1.1243

// 輸入 網內語音(sec)、網外語音(sec)、市話(sec)、網內簡訊數、網外簡訊數 測試資料。

// Sample Input：
// 500
// 120
// 13
// 2
// 5

// Sample Output：
// 183

// Sample Input：
// 200
// 200
// 200
// 200
// 200

// Sample Output：
// 383

#include <stdio.h>

int main() {
    float a, b, c;
    int x1, x2, x3, x4, x5;
    scanf("%d", &x1);
    getchar();
    scanf("%d", &x2);
    getchar();
    scanf("%d", &x3);
    getchar();
    scanf("%d", &x4);
    getchar();
    scanf("%d", &x5);
    getchar();
    a = (x1*0.08)+(x2*0.1393)+(x3*0.1349)+(x4*1.1287)+(x5*1.4803);
    b = x1*0.07+x2*0.1304+x3*0.1217+x4*1.1127+x5*1.2458;
    c = x1*0.06+x2*0.1087+x3*0.1018+x4*0.9572+x5*1.1243;
    if (a < 183)
        a = 183;
    if (b < 383)
        b = 383;
    if (c < 983)
        c = 983;
    if (a < b) {
        if (a < c) {
            printf("183");
        }
        else {
            printf("983");
        }
    }
    else {
        if (c < b) {
            printf("983");
        }
        else {
            printf("383");
        }
    }
    return 0;
}